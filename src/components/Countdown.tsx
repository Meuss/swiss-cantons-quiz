import Button from "@mui/material/Button";

const Countdown = () => {
  return (
    <div className="flex flex-col">
      Countdown
      <Button variant="contained">Give Up</Button>
    </div>
  );
};

export default Countdown;
